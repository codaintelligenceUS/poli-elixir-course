# Hello

**Learning goals**
- Run `iex` and `mix test`
- Define a module and a function
- Use doctests

**Your task**
Implement `Hello.greet/1` that returns `"Hello, <name>!"`.
- Accept a binary `name`.
- Trim surrounding whitespace.
- Capitalize only the first character; keep the rest as-is.

## Running
```bash
./scripts/run.sh 00_hello
